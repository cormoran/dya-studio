# <ページ / モジュール名>

## 範囲・根拠

- 種別: ページ / 機能 / 共有モジュール。
- 入口と利用者: URL、画面上の入口、consumer。
- 確認: 日付、対象 commit、コード確認 / UI 確認の範囲。
- 根拠表: `S1` → 相対リンク、symbol、何の根拠か。テストも別根拠として列挙。
- 関連仕様: 親・子・共有契約のリンク。

## 機能要求

| ID     | できるべきこと           | 出典・確度                                 |
| ------ | ------------------------ | ------------------------------------------ |
| XX-R01 | ユーザーの目的と成功条件 | 明示要求 / 実装から推定 / 提案。出典を記載 |

## 前提・状態

接続、Demo/実機、対応 subsystem、ロック、初期データ、言語。到達手順と loading / ready / empty / unsupported / dirty / error の見分け方。該当しない状態は理由。

## 現行の機能仕様

| ID     | 前提 → 操作              | 観測できる結果     | 保存範囲・副作用    | 根拠      |
| ------ | ------------------------ | ------------------ | ------------------- | --------- |
| XX-001 | 具体的な状態と画面ラベル | 文言・値・有効状態 | 適用 / 永続化の時点 | S1 symbol |

## 代表ユーザーフロー

各フローに目的、対象 ID、準備、番号付き操作と各期待結果、後始末を書く。成功だけでなくキャンセル・境界・復帰を含める。

## 不変条件

ID、起こってはいけない具体的なこと、確認方法、要求/実装上の防御の根拠。

## エラーと復帰

発生条件、表示箇所/文言、残る状態、再試行/復帰、Demo で到達できるか。到達不能は実機/自動テスト等での検証課題として残す。

## 探索の観点

リスク順に 3–6 個。操作を少し変える、境界、素早い繰り返し、キャンセル、ページ往復、reload、言語・狭い表示など。単なる手順再生で終わらない。

## 既知の受け入れ済み不具合

承認根拠を確認できたものはなし / ID・症状・範囲・回避策・承認根拠・再評価条件。

## 未解決・未検証

仕様の曖昧さ、不具合候補、実機依存、テスト環境の制約を分ける。未検証を受け入れ済み不具合に移さない。
